# Language

* Write in `rust` as a language

# User Interface

* Use `gtk4` for user interface framework

# Code style

* use `thiserror` for creating custom error types
* use `tracing` and `tracing_subscriber` for logging
